import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.godsgracetech.drrapha',
  appName: 'Dr Rapha',
  webDir: 'www',
  bundledWebRuntime: false,
};

export default config;
